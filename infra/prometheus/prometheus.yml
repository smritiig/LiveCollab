global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - /etc/prometheus/livecollab-rules.yml

scrape_configs:
  - job_name: livecollab-backends
    metrics_path: /metrics
    static_configs:
      - targets:
          - backend-a:8080
          - backend-b:8080
